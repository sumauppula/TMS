import React from "react";
import "./HeroBanner.css"; // Make sure this file exists and is in the same folder

const HeroBanner = () => {
  return (
    <section className="hero">
      <div className="hero-content">
        <h1>TITAN BESTSELLERS</h1>
        <p>Explore our top-selling watches now.</p>
        <button>SHOP NOW</button>
      </div>
    </section>
  );
};

export default HeroBanner;


